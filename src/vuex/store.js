import { createStore } from 'vuex';

const store = createStore({
  state() {
    return {
      // 在此处定义你的状态
    };
  },
  mutations: {
    // 在此处定义 mutations
  },
  actions: {
    // 在此处定义 actions
  },
  modules: {
    // 在此处定义子模块
  }
});

export default store;
